schema: 1
cop: "Style/TimeNow"
matchers:
  time_new:
    pattern: |
      (call (const {nil? cbase} :Time) :new)
hooks:
  - on: [send]
    match: time_new
    when: { any_of: { over: "descendants(99)", var: d, body: true } }
    offense:
      location: node
      message: "Prefer `Time.now`."
