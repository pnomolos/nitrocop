schema: 1
cop: "Style/TimeNow"
matchers:
  time_new:
    pattern: |
      (call (const {nil? cbase} :Time) :new)
    params: [Nope]
hooks:
  - on: [send]
    match: time_new
    offense:
      location: node
      message: "Prefer `Time.now`."
