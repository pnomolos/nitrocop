schema: 1
cop: "Style/TimeNow"
matchers:
  time_new:
    pattern: |
      (call (const {nil? cbase} :Time) :new)
hooks:
  - on: [send]
    match: time_new
    when: { pred: [node, "definitely_not_a_predicate?"] }
    offense:
      location: node
      message: "Prefer `Time.now`."
