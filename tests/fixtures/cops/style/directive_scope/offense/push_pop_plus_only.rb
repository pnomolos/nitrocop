# rubocop:disable Metrics/AbcSize
# rubocop:push +Metrics/AbcSize -- reviewed
^ Style/DirectiveScope: Use `enable-next` instead of `push`/`pop` around a single statement.
def foo
end
# rubocop:pop
# rubocop:enable Metrics/AbcSize
