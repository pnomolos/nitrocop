# rubocop:disable Metrics/AbcSize
# rubocop:enable Metrics/AbcSize
^ Style/DirectiveScope: Use `enable-next` instead of an `enable`/`disable` pair around a single statement.
def foo
end
# rubocop:disable Metrics/AbcSize
# rubocop:enable Metrics/AbcSize
